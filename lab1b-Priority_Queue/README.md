# 📘 Лабораторная работа 1.В  
## Очередь с приоритетами (Priority Queue)

**Курс:** ООП, ФИТ НГУ  
**Язык:** C++20

---

## 📌 Описание задачи

Реализовать класс `PriorityQueue` для хранения целых чисел с возможностью работы в двух режимах:

- **max-heap** — элемент с наибольшим приоритетом сверху (режим по умолчанию)
- **min-heap** — элемент с наименьшим приоритетом сверху

Необходимо обеспечить:

- `push(x)` — вставка за `O(log n)`
- `pop()` — удаление максимума/минимума за `O(log n)`
- `top()` — доступ к элементу за `O(1)`
- выбрасывание исключения `std::out_of_range` при `top()` и `pop()` на пустой очереди
- операторы `==` и `!=`
- вывод в поток через `operator<<`
- полное покрытие всех публичных методов **юнит-тестами Google Test**

---

## 🧱 Структура проекта

```
lab1b/
│
├─ src/
│   ├─ main.cpp
│   ├─ PriorityQueue.cpp
│   └─ PriorityQueue.hpp
│
├─ tests/
│   └─ PriorityQueueTests.cpp
│
├─ CMakeLists.txt
└─ README.md
```

---

## ⚙️ Сборка проекта

### Требования

- CMake 3.16+
- MinGW / MSYS2 g++
- Интернет (GoogleTest скачивается автоматически через FetchContent)

---

### Шаги сборки

```bash
cd lab1b
mkdir build
cd build

cmake -G "MinGW Makefiles" ..
cmake --build .
```

После сборки будут созданы:

```
lab1b.exe     — демонстрационная программа
tests.exe     — юнит-тесты Google Test
```

---

## ▶️ Запуск

### Демонстрация

```bash
./lab1b.exe
```

### Юнит-тесты

```bash
./tests.exe
```

или:

```bash
ctest
```

---

## 🧩 Реализация PriorityQueue

Очередь реализована на основе `std::vector<int>` и бинарной кучи.

Используются две основные операции:

- `sift_up(index)`
- `sift_down(index)`

Сравнение зависит от режима работы (max/min):

```cpp
compare(a, b):
    return max_heap ? (a > b) : (a < b)
```

---

## 📚 Публичный интерфейс

```cpp
PriorityQueue();
explicit PriorityQueue(bool max_heap);
explicit PriorityQueue(const std::vector<int>& data, bool max_heap = true);

void push(int x);
void pop();
const int& top() const;

bool empty() const noexcept;
size_t size() const noexcept;
void clear() noexcept;
void reserve(size_t n);

bool is_max_heap() const noexcept;

bool operator==(const PriorityQueue& rhs) const noexcept;
bool operator!=(const PriorityQueue& rhs) const noexcept;

friend std::ostream& operator<<(std::ostream&, const PriorityQueue&);
```

---

## 🧪 Юнит-тесты Google Test

Покрывают:

- вставку (`push`)
- удаление (`pop`)
- доступ (`top`)
- все конструкторы
- копирование и перемещение
- сравнение `==`, `!=`
- очистку (`clear`)
- проверку режима `is_max_heap()`
- исключения на пустой очереди

Результат:

```
[  PASSED  ] 12 tests.
```

---

## ✔️ Статус

- Реализация соответствует требованиям
- Все публичные методы протестированы
- GoogleTest подключён автоматически
- Сборка кросс-платформенная через CMake

---

